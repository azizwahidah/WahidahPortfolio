{
  "name": "wahidah-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
